import { FaSpinner } from 'react-icons/fa'
import './Spinner.css'

const Spinner = () => {
    return (
        <span className='spinner'>
            <FaSpinner/>
        </span>
    )
}

export default Spinner
